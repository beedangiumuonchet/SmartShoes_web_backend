spring:
  # Development Database Configuration
  datasource:
    url: jdbc:mysql://localhost:3306/shoesstore_db
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 123456

  # JPA Configuration for Development
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect

# Development Logging - Only DB, errors, and app logs
logging:
  level:
    com:
      ds: INFO
    org:
      springframework:
        security: WARN
        # SQL: DEBUG
        # type.descriptor.sql.BasicBinder: TRACE
        web: WARN
        boot: WARN
        data: WARN
        transaction: WARN
        orm: WARN
      hibernate:
        SQL: WARN
        engine:
          jdbc:
            spi:
              SqlExceptionHelper: WARN
    root: WARN
  # Disable Hibernate query logging
  org.hibernate.SQL: WARN
  org.hibernate.type.descriptor.sql.BasicBinder: WARN

# Development Configuration
app:
  admin:
    email: admin@mail.mail
    username: admin
    password: admin

momo:
  partner-code: MOMO
  access-key: F8BBA842ECF85
  secret-key: K951B6PE1waDMi640xX08PD3vg6EkVlz

  # Endpoint
  create-endpoint: https://test-payment.momo.vn/v2/gateway/api/create
  query-endpoint: https://test-payment.momo.vn/v2/gateway/api/query

  # Callback URLs
  return-url: https://a1056bc73497.ngrok-free.app/api/payments/momo/return
  ipn-url: https://a1056bc73497.ngrok-free.app/api/payments/momo/ipn

  # Request Type
  request-type: payWithMethod